# Current Focus

See `.lovable/roadmap.md` for the full roadmap.

## Next Up: Phase 1 — Fixes

1. **1.3 Tool Registry** — Create `src/lib/tool-registry.ts`, refactor Navbar/CommandPalette/Footer/Index
2. **1.1 Date/Timestamp Display** — Update `DataTable.formatValue()` for DuckDB date types
3. **1.2 Failing Tests** — Make route count assertions dynamic via registry
4. **1.4 Remove Space Mono** — Clean up font declarations
5. **1.5 Split seo-content.tsx** — Per-tool dynamic imports
